export type accountType = {
  userId: string;
  password: string;
};

export type panelInfoType = {
  modelName: string;
  maker: string;
  serialNum: string;
};

export type installInfoType = {
  installDate: string;
  installLocation: string;
  initialPower: number;
};
